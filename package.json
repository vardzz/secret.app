{
  "name": "secret-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd backend && npx tauri dev",
    "build": "cd backend && npx tauri build",
    "tauri": "cd backend && npx tauri"
  }
}
